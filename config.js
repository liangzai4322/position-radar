/* Deployment configuration. Keep this file free of credentials. */
window.APP_CONFIG = Object.freeze({
  API_BASE_URL: 'https://liangzai666.com',
  STATUS_PATH: '/api/status',
  REQUEST_TIMEOUT_MS: 12000,
  STALE_AFTER_SECONDS: 45,
  MIN_POLL_SECONDS: 10
});
